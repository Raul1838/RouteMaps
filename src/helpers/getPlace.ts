import { Coords } from "../interfaces/Coords";
import Place from "../interfaces/Place";

export const getPlace = async( coords: Coords ): Promise<Place> => {
   throw new Error("Not implemented yet");
}
 